import { InputValDirective } from './input-val.directive';

describe('InputValDirective', () => {
  it('should create an instance', () => {
    const directive = new InputValDirective();
    expect(directive).toBeTruthy();
  });
});
